import { ConfigServerProvider } from './config-server-provider';

describe('ConfigServerProvider', () => {
  it('should create an instance', () => {
    expect(new ConfigServerProvider()).toBeTruthy();
  });
});
