import { ServiceTourismEntity } from './service-tourism-entity';

describe('ServiceTourismEntity', () => {
  it('should create an instance', () => {
    expect(new ServiceTourismEntity()).toBeTruthy();
  });
});
